enrutamiento inter vlan switch - redes ip conmutadas instructor ing luis escudero escuderoluisalbertogmailcom objetivos completar modulo lograremos * configurar router externo enrutamiento inter vlan * configurar enrutamiento switch multicapa implementacion enrutamiento inter vlan utilizando router enrutamiento inter vlan * explicar funcion enrutamiento inter vlan router externo * configurar enrutamiento inter vlan router externo verificar operacion * describir ventajas desventajas configurar enrutamiento inter vlan router externo enrutamiento inter vlan router externo * proveer unico enlace trunk switch router * interface fisica vlans muestran logicamente router dividida separadas solo interface f fisica router eth0 0 subinterfaces locales subinterfaces eth0 010 - vlan 10 data * cada eth0 020 - vlan 20 data subinterfaces actua gateway vlan discovery 10 enrutamiento router externo enrutamiento router externo configurar subinterfaces tal forma r1 pueda rutear pc1 vlan10 pc2 vlan20 configurar trunk forma tal r1 reciba trafico necesita ser ruteado configuracion subinterfaces router * configurar subinterface vlan 10 * configurar subinterface vlan 20 * configurar subinterface vlan 1 vlan nativa verificacion subinterfaces router * verificar todas subinterfaces status protocolo < < up > > configuracion trunk switch * configurar trunk enlace r1 vlan 10 vlan 20 encapsulacion trunk debe coincidir subinterfaces router verificacion trunk switch * verificar link sw1 r1 transportando trafico vlan 10 vlan 20 flujo trafico router on stick verificar flujo trafico * verificar trafico pc1 pc2 router externo ventajas desventajas ventajas desventajas funciona cualquier switch router unico punto falla requiere servicio switch capa 3 implementacion simple unico camino trafico puede congestionar router provee comunicacion puede introducir latencia debido vlans tramas dejan switch entran router vuelven salir varias veces router toma decisiones base software resumen * configurar router externo * trunk debe configurarse switch router externo * subinterface router debe configurado cada vlan * ventajas * implementacion simple * router provee comunicacion vlans * desventajas * router unico punto falla * puede haber congestion latencia laboratorio challenge 8 configurar enrutamiento vlans router configurar switch ruteo enrutamiento inter vlan generalidades * describir svi switch virtual interface * describir rol puerto ruteado switch * configurar svi switch * configurar puerto ruteado switch * configurar protocolo enrutamiento switch multicapa * configurar svi comando autostate exclude * compara etherchannel capa 2 capa 3 * configurar etherchannel capa 3 svi switch virtual interface * router utilizan interfaces subinterfaces interconectar multiples vlans * switches multicapa utilizan svi enlutar vlans svi configurado switch multicapa puede crear svi paca cualquier vlan exista switch solo svi puede asociar vlan svi puede configurar operar capa 2 capa 3 puertos switch ruteo * puertos switch capacidad capa 3 interfaces * debe asociado ruteadas ninguna vlan * requiere remover funcionalidad capa 2 * configura interface router < < soporta subinterface vlan > > * utiliza switch puerto vlan solamente subredes * util enlaces punto punto capa 3 discovery 11 routing switch multicapa objetivo topologia * configurar svi enrutamiento inter vlan manera dsw1 pueda enrutar pc1 pc2 * configurar puertos ruteados eigrp manera dsw1 pueda obtener rutas r1 configuracion svi crear vlans configurara svis habilitar enrutamiento ip switch multicapa configuracion svi cont dsw1 configurar svi vlan 10 dsw1 configurar svi vlan 20 verificar enrutamiento inter vlan verificar enrutamiento inter vlan cont configuracion puerto ruteado dsw1 cambiar interface conecta r1 ethernet 0 0 interface ruteada configuramos direccion ip 100991 24 configuracion puerto ruteado cont dsw1 verificar configuracion puerto ruteado configurar protocolo enrutamiento verificacion * dsw1 configurar eigrp as1 habilitar vlan10 vlan20 ethernet 0 2 eigrp * verificar tabla enrutamiento dsw1 comando svi autostate exclude svi muestra arriba up cumplen siguientes * vlan existe activa base datos vlan switch * existe interface vlan administrativamente abajo down * menos puerto capa 2 acceso trunk existe link arriba up reenvio spanning tree vlan * svi autoestate exclude remueve puerto calculo line state up and down lista chequeo configuracion svi * identificar vlans requieren gateway capa 3 * crear vlan switch multicapa si existe * crear svi cada vlan * configurar svi direccion ip * habilitar svi * habilitar enrutamiento ip switch multicapa * determinar si necesario protocolo enrutamiento dinamico * configurar protocolo enrutamiento si necesario * identificar puertos switch requieran comando autostate exclude * configurar autostate exclude puertos identificados switch etherchannel capa 2 vs etherchannel capa 3 * grupos puertos ruteados etherchannel capa 3 * grupos puertos acceso trunk etherchannel capa 2 configuracion etherchannel capa 3 * comando switchport aplica puerto fisico interface etherchannel * interfaces sw2 debe configurada misma manera configuracion etherchannel capa 3 cont * verificar etherchannel capa 3 levantado up resumen * comunicacion inter vlan requiere dispositivo capa 3 router externo switch multicapa * svi interface virtual provee procesamiento capa 2 vlan particular similar hace interface router * svi active numero svi debe coincidir vlan existente switch local * comando switchport convierte interface switch interface ruteada * switches multicapa puede agregar enlaces capa 3 utilizando etherchannel capa 3 laboratorio challenge 9 configurar enrutamiento switch multicapa resumen modulo * configurar router externo trunk debe ser configurado switch router externo debe configurar subinterface cada vlan * configurar switch multicapa enrutamiento inter vlan crear vlan crear svi configurar direccion ip habilitarla habilitar enrutamiento ip enrutamiento inter vlan switch - redes ip conmutadas